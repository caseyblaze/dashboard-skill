---
name: dashboard
description: Build dashboards and charts with a fixed light-business palette, fixed layout grid, and consistent chart-selection logic. Use this whenever the user asks for a dashboard, chart, graph, plot, report visual, KPI view, trend/comparison/breakdown of numbers, or mentions 儀表板、圖表、繪圖、視覺化、報表、趨勢圖、佔比. Also use it when the user hands over a CSV/Excel/query result and asks "看一下這份資料" or wants numbers presented — even if they never say the word "dashboard". Covers both interactive HTML output and static PNG output.
---

# Dashboard

固定配色 + 固定版型的儀表板產出流程。目的是每次做出來的東西長得一樣、看得懂、不用重新討論美感。

## 開工前先定兩件事

**1. 輸出成 HTML 還是圖檔?** 依用途判斷,不確定就問一句:

| 用途 | 輸出 |
|---|---|
| 要 hover / 篩選 / 切換、要在對話裡直接看、要分享連結 | HTML |
| 要貼進簡報、文件、Email、列印 | PNG |
| 只有 1 張圖、資料 < 20 點 | PNG(比較省事) |

**2. 資料先看過再畫。** 不要拿到欄位名就直接畫。

- CSV / Excel → pandas 讀進來,先看 `shape`、`dtypes`、缺值數、數值欄位的 min/max。日期欄位要真的轉成 datetime。
- API / 資料庫 → 先取前 20 筆看結構,確認聚合的顆粒度(日?週?月?)。
- 對話裡貼的數字 → 直接寫進資料陣列,但要回頭確認單位跟時間範圍。

發現資料有問題(缺月份、極端值、單位不一致)先講,不要默默畫出一張騙人的圖。

## 判斷基準

下面的配色、版型、選圖規則已經定好,照做就行。**遇到規則沒寫到的情況,依這個順序裁決,排前面的優先**:

1. **誠實**。圖形上的變化幅度要等於資料的變化幅度(lie factor = 1)。會放大或縮小差距的畫法一律不用,再好看也不用。
2. **感知準確度**。要讓人比較數值,就用判讀精度排前面的編碼:共同基準的位置 > 同軸不同基準的位置 > 長度 > 角度/斜率 > 面積 > 體積 > 顏色深淺。這是實證排序(Cleveland & McGill),不是品味,所以「長條贏圓餅」沒有討論空間。
3. **前注意處理**。顏色、大小、方向、位置這四種屬性,人在 200ms 內不用逐一掃描就處理完 —— 所以重點只能有一個、其餘灰化才有效。分組靠間距與位置(接近、相似法則),不要靠框線。
4. **精簡**。不承載資訊的墨水全部刪掉:外框、垂直格線、陰影、3D、漸層填充。與其疊十條線,不如切成十張共用刻度的小圖。
5. **敘事**。先確定「要對誰講什麼」再動手。標題寫結論不寫欄位名。類別超過 5–7 個就超出短期記憶,併成「其他」。

衝突時前面的贏。漂亮但會誤導的圖,直接換一種畫法。

## 配色(固定,不要即興發揮)

```
背景        #FFFFFF
卡片底      #F9FAFB
邊框        #E5E7EB
主文字      #101828
次文字      #667085
格線        #EEF0F3

資料色序(照順序用,不要跳號)
1 #2563EB  2 #0E9F6E  3 #F59E0B  4 #7C3AED  5 #DC6803  6 #64748B

正向 #0E9F6E   負向 #DC2626   中性 #667085
單色漸層 #DBEAFE → #93C5FD → #3B82F6 → #1D4ED8 → #1E3A8A
```

用色原則:

- **一張圖只有一個重點**。要強調某一根/某一條就只給它 `#2563EB`,其餘全部 `#CBD5E1`。顏色是前注意屬性,一眼就抓到;全部上色等於沒有重點。灰化比多上顏色有效。
- 色序是給**類別**用的。有序資料(分數、密度、熱力圖)用單色漸層。
- **顏色深淺的判讀精度排在最後一名**,只用在「看型態不看數值」的場合(熱力圖、地圖密度)。要讓人讀出數值或精確比較大小,一律回到位置與長度。同理,不要用氣泡面積表示重要數字。
- **同一個序列在所有圖裡用同一個顏色**。主圖的藍色是「北區」,輔圖的藍色就不能變成「線上通路」,否則相似法則會讓人自己接錯線。
- 上一條跟「強調色用 `#2563EB`」衝突時,**身份顏色優先**。某個實體在 A 圖是被強調的那一根、在 B 圖是堆疊裡的一段,就讓它在兩張圖都用同一個色(必要時強調色改用 `#F59E0B`),不要為了照色序而讓同一個東西換色。
- 漲跌才用紅綠,類別不要用紅綠——會被誤讀成好壞。
- 超過 6 個類別就不要再加色(短期記憶大約 5–7 項),改成排序後只標前 5 名 +「其他」。

## 版型(固定,由上往下)

```
標題列    H1 20px/600 + 一行灰色副標(資料範圍、單位、更新時間)
KPI 列    3–5 張卡:標籤 13px 灰 / 數值 28px 600 / 比較基準 13px(紅綠 ▲▼ 或灰字)
主圖      佔滿寬,高 320–400px,回答最主要的那個問題
輔圖      2 欄(窄螢幕自動變 1 欄),高 240–280px
明細      表格放最後,預設收合或只顯示前 10 列
```

- 間距一律 8 的倍數:卡片內距 20、區塊間距 24、圓角 8、邊框 1px。
- **組內間距要明顯小於組間間距**。人靠接近程度分群,不是靠框線。同一組的卡片靠緊、不同區塊拉開,就不必再加外框。
- **標題 + KPI + 主圖要在第一屏看完,不用捲動**。輔圖和明細可以放下面,但「這份資料在講什麼」不能藏在捲軸下方。塞不下就是圖太多,砍掉不是最重要的那幾張。
- 手機寬度(< 640px)全部退成單欄,KPI 變 2 欄。
- 每張圖上方要有一句**結論式標題**,不是「月營收」而是「營收連 3 個月成長,Q3 比 Q2 高 18%」。圖表的價值在這句話。

### KPI 卡的比較基準

**單獨一個數字沒有意義。**「298」無法讓人判斷是好是壞,一定要給基準。優先序:上一期 > 去年同期 > 目標 > 同儕/全體平均。

**但沒有基準就留白,絕對不要編一個。** 只有手上真的有上一期資料時才放 ▲▼ 和百分比。手上只有單期資料時,第三行改成灰字寫一句限定範圍的事實(「全月 30 天」「15 台扣款 2 次」),或直接不放。捏造一個「vs 上月 +12%」比不比較更糟 —— 那是假資訊,不是排版問題。

## 選圖邏輯

先問「這張圖要回答什麼問題」,再對照。同一個問題有兩種畫法時,選判讀精度排前面的那個(見判斷基準 2)。

| 問題 | 圖 | 注意 |
|---|---|---|
| 隨時間怎麼變 | 折線 | 超過 4 條就拆成 small multiples 或灰化其他線 |
| 隨時間怎麼變,但有零值或缺口 | 長條 | 折線會把缺口連成一條貼地直線,看起來像「低但有在跑」。離散計數(筆數、件數)本來就該用長條 |
| 誰比較多 | 橫向長條 | **一定要排序**,類別名長時更要用橫的 |
| 各期組成變化 | 堆疊長條 | **只有最底層那段是共同基準,能精確比較**。最想讓人比較的序列放最底下,其餘只能看趨勢 |
| 佔比 | 100% 堆疊長條 | 圓餅只在 ≤ 3 類且差距明顯時用(角度的判讀精度低於長度) |
| 同一指標跨多個分組 | small multiples | 共用同一組刻度,否則讀者會直接比形狀而得到錯的結論 |
| 兩個變數有沒有關係 | 散布圖 | 點多就加透明度,不要加趨勢線除非有意義 |
| 分布長怎樣 | 直方圖 / 箱型圖 | 平均值會騙人,分布不會 |
| 對不對得上目標 | 長條 + 目標虛線 | 虛線用 `#667085` |

## 呈現規則

這幾條是最常出錯的地方:

- **長條圖 y 軸一定從 0 開始**,截斷會放大差距。折線圖可以不從 0,但要讓刻度看得出來。
- **不要雙 Y 軸**。兩條線的交叉點是人為產物 —— 只要調整任一邊的縮放,交叉點就會移動,等於讓排版決定結論。真的要比兩個不同單位,改成上下兩張對齊 x 軸的圖,或都換算成指數(基期 = 100)。
- **沒有資料不等於 0**。缺漏要留成缺口或明確標註,不要內插、不要補 0 混進總數與平均。發現整段缺漏(連續多期為零、單期佔比異常高)先寫在圖下方再畫圖。
- **用面積編碼時只縮放面積本身**。把寬和高同時乘上倍數會讓差距平方化,2 倍看起來像 4 倍。
- **small multiples 的每張小圖必須共用同一組刻度**。各自 autoscale 會讓形狀看起來一樣,實際上量級差十倍。
- 3 條線以內用**直接標註**(線末端寫名字)取代圖例;4 條以上才放圖例,放右上。
- 格線只要水平、`#EEF0F3`、1px。垂直格線、外框、陰影、3D、漸層填充一律不要。
- 數字加千分位;金額標單位在軸標題不要每個刻度都寫;百分比統一小數點後 1 位。
- 時間軸由左到右,最新在右邊。
- 資料點少於 5 個就別畫圖了,直接列數字比較快。

## 圖表旁邊的文字

圖畫對了,字寫壞了一樣沒人看。每張圖的文字只有兩層:

```
結論式標題   一句話,講結論不講欄位名
說明文字     選擇性,上限 150 字
```

- **150 字是硬上限**,不是目標。超過就不是儀表板是報告,讀的人會整段跳過,連帶跳過旁邊的圖。寫完超過了就刪,不要縮小字級硬塞。
- **講人話,不要講資料處理的話**。「欄位」「拆解」「聚合」「維度」「批次」「顆粒度」這些是你的工作語言,不是讀者的。
  - ✗ `name 欄位為單一值,無法做類別拆解` → ✓ `這批資料只有一種交易類型,分不出類別`
  - ✗ `6/20 出現批次補跑` → ✓ `6/20 像是停擺後一次補回來`
- **講結論,不要講過程**。怎麼算出來的、排除了哪些條件,放附註或口頭講,不要佔版面。
- **一次只講一件事**。同一段裡要講三件事,代表這應該是三張圖。
- 標題已經講完的不要在說明裡再講一次,那是湊字數。

## 產出

讀對應的參考檔再動手,不要兩個都讀:

- HTML → `references/html.md`
- Python 出圖 → `references/python.md`

做完先自己檢查一遍:

**誠實**
- [ ] 資料缺口有沒有被畫成 0,或被平均掉
- [ ] 長條 y 軸從 0,沒有雙 Y 軸
- [ ] 有沒有拿面積或顏色深淺去編碼需要精確比較的數字
- [ ] KPI 有基準才比較;沒有基準的是留白,不是編一個

**讀得懂**
- [ ] 每張圖的標題是結論句,不是欄位名
- [ ] 每段說明文字都在 150 字以內(數過,不是感覺)
- [ ] 說明裡沒有欄位、拆解、聚合、批次這類工作術語
- [ ] 長條有排序
- [ ] 一張圖只有一個重點,其餘灰化
- [ ] 同一個序列在不同圖裡是同一個顏色

**版面**
- [ ] 標題 + KPI + 主圖在第一屏看得完
- [ ] 手機寬度不爆版
